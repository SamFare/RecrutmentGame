{
  "name": "skillsmatchergame",
  "version": "1.0.0",
  "description": "A Game to match desired skills with a candidate",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p build && cp {style.css,index.html,Main.js} build && cp -a assets build && concat-cli -f src/**/*.js src/Game.js -o build/script.js && karma start",
    "start": "npm run build && http-server ./build/"
  },
  "dependencies": {},
  "devDependencies": {
    "concat-cli": "^4.0.0",
    "http-server": "^0.11.1",
    "jasmine": "^3.3.1",
    "karma": "^4.0.0",
    "karma-chrome-launcher": "^2.2.0",
    "karma-jasmine": "^2.0.1",
    "uglify-js": "^2.6.2"
  },
  "author": "Sam Fare",
  "license": "ISC"
}
